# Elixir is a scripting language. 

Install Elixir support onto your computer using Homebrew, then run programs using `elixir`. Really could not be more easy. 

`.ex` files are compiled, `.exs` files are for scripting.